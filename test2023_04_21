#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
#include <stdlib.h>
#include <windows.h>
//int main()
//{
//	int* p = (int*)malloc(40);
//	if (p == NULL)
//	{
//		return 0;
//	}
//	//使用
//	int* p2=(int*)
//
//	return 0;
//}

//int main()
//{
//	int* p = (int*)malloc(40);//10个int
//	if (p == NULL)
//	{
//		return 0;
//	}
//	int i = 0;
//	for (i = 0; i < 10; i++)
//	{
//		*(p + i) = i;
//	}
//	//回收空间
//	free(p);
//	p = NULL;
//	return 0;
//}

//int main()
//{
//	int* p = (int*)malloc(40);//10个int
//	if (p == NULL)
//	{
//		return 0;
//	}
//	int i = 0;
//	for (i = 0; i < 10; i++)
//	{
//		*p++ = i;
//	}
//	//回收空间
//	//free必须从开辟空间的起始地址开始释放，
//	free(p);
//	p = NULL;
//	return 0;
//}

//对同一块动态内存的多次释放
//int main()
//{
//	int* p = (int*)malloc(40);
//	if (p == NULL)
//	{
//		return 0;
//	}
//	//使用
//	
//	//释放
//	free(p);
//	p = NULL;
//	free(p);
//	return 0;
//}

int main()
{
	//6.动态开辟内存忘记释放（内存泄露）
	while (1)
	{
		malloc(1);//开辟一个字节
		Sleep(1000);
	}


	return 0;
}
