#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
int main()
{
	int a = 10000;
	FILE* pf = fopen("test.txt", "wb");
	fwrite(&a, 4, 1, pf);//写一个二进制文件
	fclose(pf);
	pf = NULL;
	return 0;
}
//文件缓冲区





